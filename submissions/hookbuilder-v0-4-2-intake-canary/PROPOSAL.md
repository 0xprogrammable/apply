# Proposal

This inert proposal exercises the released Hookbuilder v0.4.2 transport. It describes one token and one immutable pool-scoped fee hook, but contains no deployed code and makes no approval, availability, or launch claim.
